{% block _product_datagrid_row_product_badge_widget %}
    {% if
        product is defined and
        product.badges is defined and
        product.badges|length
    %}
        {% set badges = product.badges %}
    {% endif %}
    {{ dump(product) }}
    {{ block_widget(block, {'badges' : badges|default({})}) }}
{% endblock %}